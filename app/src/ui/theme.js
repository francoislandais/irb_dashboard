export const primaryDark = getComputedStyle(document.documentElement)
  .getPropertyValue("--primary-dark")
  .trim();

export const flowArrowColor = getComputedStyle(document.documentElement)
  .getPropertyValue("--flow-arrow")
  .trim();
